apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloudinfinit-apigw.fullname" . }}
  labels:
    {{- include "cloudinfinit-apigw.labels" . | nindent 4 }}
spec:
  type: {{ .Values.platform.service.type }}
  ports:
    - port: {{ .Values.platform.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cloudinfinit-apigw.selectorLabels" . | nindent 4 }}
